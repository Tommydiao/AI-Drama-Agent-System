# 02_DECISIONS.md — AI 短剧制作 Agent 关键决策记录

> **文档性质：已批准的产品与技术方向决策基线**  
> 本文件把已经确认的默认方案固化为可追溯决策。后续 PRD、架构、数据契约、状态机、实施计划与代码必须遵守这些决策；需要改变时，应新增 Decision Record，而不是静默覆盖。

---

## 0. 文档控制

| 字段 | 内容 |
|---|---|
| 项目名称 | AI 短剧制作 Agent |
| 文档版本 | v0.1 |
| 状态 | **Approved** |
| 上位文件 | `INTENT.md v0.1` |
| 决策人 | Product Owner |
| 批准时间 | 2026-08-29 |
| 研发模式 | Human-Governed, AI-Operated, Evidence-Gated |

## 0.1 决策优先级

当文档之间发生冲突时，优先级为：

```text
INTENT.md
→ 02_DECISIONS.md
→ PRODUCT_SPEC / USER_FLOWS
→ ARCHITECTURE / DATA_CONTRACTS / STATE_MACHINES
→ IMPLEMENTATION_PLAN
→ 代码与测试
```

## 0.2 决策变更方式

任何已批准决策需要调整时，必须记录：

- 原决策
- 新决策
- 修改原因
- 影响范围
- 迁移或返工成本
- 新的验收要求
- 批准人和批准时间

---

# 1. 决策摘要

| ID | 决策 | 结论 | 状态 |
|---|---|---|---|
| DEC-001 | 产品形态 | 先做个人创作工具，架构预留 SaaS 化 | Accepted |
| DEC-002 | 核心用户 | Product Owner 本人及个人 AI 内容创作者 | Accepted |
| DEC-003 | MVP 规格 | 60–90 秒、9:16、720P/30fps、12–24 镜头 | Accepted |
| DEC-004 | 角色与场景 | 最多 2 个主要角色、1–2 个核心场景 | Accepted |
| DEC-005 | 基准短剧 | 使用《门外的人》作为首条系统验收样片 | Accepted |
| DEC-006 | 自动化模式 | 默认一键运行至完整草片，异常才介入 | Accepted |
| DEC-007 | 模型接入 | 云 API 优先，所有模型必须通过 Provider Adapter | Accepted |
| DEC-008 | LoRA 与本地 GPU | MVP 不训练 LoRA，不建设大型本地 GPU 集群 | Accepted |
| DEC-009 | 开发策略 | Mock-first，完整闭环通过后再接真实付费模型 | Accepted |
| DEC-010 | 技术栈 | Next.js、FastAPI、PostgreSQL、Temporal、FFmpeg | Accepted |
| DEC-011 | 文件存储 | 开发期本地存储，后续切换 OSS/S3 | Accepted |
| DEC-012 | 自动修复 | 每个镜头最多自动修复 2 次 | Accepted |
| DEC-013 | 首轮模型预算 | 500 元人民币 | Accepted |
| DEC-014 | 首个里程碑 | 约 2 周完成 Mock 完整短剧闭环 | Accepted |
| DEC-015 | 真实模型 MVP 周期 | 目标约 7–8 周 | Accepted |
| DEC-016 | 安全与授权 | 只处理用户有合法使用权的真人与素材 | Accepted |
| DEC-017 | 完成判定 | 以测试、媒体结果和 Evidence 为准，不以代码声明为准 | Accepted |

---

# 2. 详细决策记录

## DEC-001：产品先个人化，架构预留 SaaS

**状态：** Accepted  
**日期：** 2026-08-29

### 背景

第一阶段的目标是快速验证完整短剧生产闭环，而不是同时承担账号体系、组织协作、计费、套餐、运营后台等 SaaS 复杂度。

### 决策

- MVP 先服务单个创作者。
- 不在首版实现团队协作、订阅计费、组织权限和运营后台。
- 核心数据模型预留 `workspace_id` 或等价的租户边界。
- 文件路径、成本记录、项目访问权限和任务隔离不得写死为全局单用户。
- 不能为了“预留 SaaS”在首版过度建设微服务和复杂权限系统。

### 影响

**正向：**

- 更快完成有效产品闭环。
- 产品负责人可以直接作为第一位真实用户。
- 后续迁移到 SaaS 时保留基本边界。

**代价：**

- 首版不支持多人协作和商业计费。
- 一些 SaaS 能力需要后续单独设计。

### 重新评估触发条件

- 出现第二个稳定外部用户。
- 需要多人共同审核或编辑项目。
- 需要上线付费套餐。
- 需要跨工作区隔离数据与 Provider 凭证。

---

## DEC-002：第一目标用户为个人 AI 内容创作者

**状态：** Accepted

### 决策

首版优先解决以下用户的核心任务：

> 有故事创意、懂基本 AI 工具，但没有完整影视团队，希望以少量人工操作得到可观看、可修改、可追溯短剧的个人创作者。

Product Owner 本人是第一位基准用户。

### 产品后果

- 默认流程优先简单与自动，而不是面向专业剧组的极细参数控制。
- 专业参数可以展示在高级设置中，但不应阻碍默认流程。
- 成本、失败原因和局部重做必须对个人用户足够清楚。
- 产品文案避免把用户假设为专业导演、摄影师或剪辑师。

---

## DEC-003：MVP 成片规格

**状态：** Accepted

### 决策

MVP 默认成片规格：

- 时长：60–90 秒
- 画幅：9:16
- 分辨率：720P
- 帧率：30fps
- 镜头数：12–24
- 语言：中文普通话
- 首批题材：都市情感、悬疑反转、职场冲突等对话型短剧

### 原因

该范围足以验证故事、角色、场景、台词、镜头、视频生成、音频、字幕、剪辑、QC、修复和导出全链路，同时能限制生成成本和连续性难度。

### 非含义

- 这不是产品长期上限。
- 不意味着所有项目必须恰好 60 秒或固定 18 个镜头。
- 不允许首版悄然扩大到 8–10 分钟和上百镜头。

---

## DEC-004：最多 2 个主要角色和 1–2 个场景

**状态：** Accepted

### 决策

MVP 的自动生产承诺只覆盖：

- 最多 2 个主要角色
- 最多 1–2 个核心场景
- 可存在不需要稳定身份的背景人物，但不能成为关键叙事角色

### 原因

双角色、少场景已经可以验证：

- 正反打
- 人物视线
- 服装和位置连续性
- 道具换手
- 中文对白与口型
- 跨镜头情绪变化

同时避免多人互动和复杂场景切换在首版造成不可控失败。

---

## DEC-005：基准短剧采用《门外的人》

**状态：** Accepted

### 基准故事

> 夜晚，女主在公寓里发现男友带回来的钥匙并不属于他们家。男友声称整晚都在公司，但手机上出现陌生女人的消息。两人短暂对质。就在女主以为男友出轨时，门外响起敲门声，门把手缓慢转动。男友惊恐地说：“她怎么找到这里了？”

### 使用目的

该故事是**系统验收样片**，不是最终商业内容。它需要覆盖：

- 两个稳定角色
- 一个公寓主场景
- 手机和钥匙两个关键道具
- 单人特写、双人中景、正反打和空镜
- 情绪从怀疑到紧张的变化
- 中文对白、环境声和悬疑音乐
- 结尾钩子
- 单镜头局部重做

### 验收要求

后续 Acceptance Tests 必须引用该基准故事，并给出固定输入版本，避免每次测试使用不同故事而无法回归比较。

---

## DEC-006：默认一键运行，异常时才介入

**状态：** Accepted

### 决策

面向用户的默认流程：

```text
输入创意与约束
→ 启动生产
→ 系统自动生成完整草片
→ 异常时请求接管
→ 用户最终验收与导出
```

用户不应被要求在每个内部阶段连续点击“下一步”或“继续”。

### 必要人工节点

- 启动确认：目标、授权和预算。
- 异常接管：自动系统无法在既定次数和成本内解决。
- 最终确认：发布或正式交付。

### 可选人工控制

用户可以主动：

- 暂停或取消
- 修改剧本或设定
- 锁定角色、场景或镜头
- 替换候选
- 重新生成局部资产
- 调整成本与质量策略

### 研发约束

研发过程中可以使用更多内部 Gate 做验证，但这些 Gate 不能被默认暴露为最终用户的必经步骤。

---

## DEC-007：云 API 优先，Provider 可替换

**状态：** Accepted

### 决策

- MVP 优先使用云端图片、视频、语音和评价模型。
- 所有外部模型通过统一 `Provider Adapter` 调用。
- 业务层不得直接依赖特定厂商请求或返回格式。
- 关键生成任务必须记录 Provider、模型、版本、参数、输入、输出和成本。
- Provider 切换必须显式，不允许静默降级为不等价能力。

### 后果

首版重点投入：

- Provider 接口
- 任务幂等
- 错误归一化
- 成本记录
- 资产追溯

而不是提前搭建大量本地推理基础设施。

---

## DEC-008：MVP 不训练 LoRA，不建设大型 GPU 集群

**状态：** Accepted

### 决策

- 第一版使用角色参考图、Character Look、场景参考图、首尾帧和连续性状态提升一致性。
- 不把 LoRA 训练作为 MVP 前置条件。
- 不采购或依赖双专业显卡等大型本地渲染配置。
- 本地推理能力只在云 API 不能满足成本、隐私或稳定性时重新评估。

### 重新评估触发条件

- 同一角色需要稳定生产多集、数百个镜头。
- 参考图方案无法达到最低一致性指标。
- 云 API 成本明显高于自建方案。
- 存在必须私有化处理的数据。
- Provider 限流成为稳定生产瓶颈。

---

## DEC-009：Mock-first

**状态：** Accepted

### 决策

真实付费模型接入前，必须先跑通：

```text
项目创建
→ 故事
→ 剧本
→ 角色 / 场景 / 道具设定
→ Shot List / Shot Graph
→ Mock 关键帧
→ Mock 视频与音频
→ Mock QC
→ Timeline
→ FFmpeg 成片
```

### 必须先验证

- 状态机
- 任务队列
- 幂等
- 断点恢复
- 单镜头失败
- 两次修复上限
- 单镜头替换
- 台词修改影响分析
- 预算阻断
- 服务重启恢复
- Evidence 生成
- 最终导出

### 禁止事项

不得用真实模型生成成功来掩盖工作流不稳定、状态丢失或无法局部重做等系统问题。

---

## DEC-010：技术栈基线

**状态：** Accepted

### 决策

| 层级 | 技术 |
|---|---|
| Web 前端 | Next.js + TypeScript |
| 后端 API 与媒体编排接口 | FastAPI + Python |
| 业务数据库 | PostgreSQL |
| 长任务与恢复 | Temporal |
| 媒体检查和合成 | FFmpeg |
| 本地开发 | Docker Compose 优先 |
| 后续可选 | 对象存储、GPU Worker、可观测平台 |

### 边界

- LangGraph 不是首版必选项。
- 不因“Agent 项目”而强制引入多个 Agent 框架。
- Temporal 负责持久化长工作流与 Activity；LLM 规划可先使用普通 Python 节点和严格 Schema。
- 前端不直接调用付费媒体 Provider。
- FFmpeg 命令必须封装、测试并记录输入输出。

### 重新评估触发条件

Explore 阶段若以证据证明某项技术不满足关键要求，可提出 ADR，但不能直接替换。

---

## DEC-011：开发期本地存储，后续 OSS/S3

**状态：** Accepted

### 决策

- 开发期允许使用本地文件系统保存媒体资产。
- 存储访问必须通过统一 Storage Adapter。
- 数据库中保存稳定资产 ID 和元数据，不能把业务逻辑绑定到本地绝对路径。
- 后续可以切换到 OSS/S3，不影响 Shot、Asset 和 Timeline 数据结构。

---

## DEC-012：每镜头最多自动修复 2 次

**状态：** Accepted

### 决策

每个镜头的默认自动流程：

```text
首次生成
→ QC
→ Repair 1
→ QC
→ Repair 2
→ QC
→ Pass / Human Review / Degrade
```

### 规则

- “生成多个默认候选”和“修复”需要分别记账。
- 不允许通过改变任务 ID 绕过修复次数。
- 修复应基于具体问题代码。
- 两次修复仍失败时，系统必须给出：
  - 失败原因
  - 已尝试策略
  - 当前成本
  - 推荐人工动作
  - 可选降级方案

---

## DEC-013：首轮真实模型预算为 500 元

**状态：** Accepted

### 决策

首轮真实 Provider 验证设置项目级预算池：

```text
CNY 500
```

### 使用顺序

1. 关键帧与静态资产小样
2. 少量代表性镜头
3. 基准短剧低质量候选
4. 选中镜头的目标质量版本
5. 必要的有限修复

### 预算控制

- 每次付费调用前检查剩余额度。
- 达到警戒阈值时提示。
- 预计超限时暂停。
- 未经 Product Owner 批准不得增加预算。
- 预算用完不等于功能验收通过。

---

## DEC-014：约 2 周完成 Mock 里程碑

**状态：** Accepted

### 里程碑定义

“Mock 里程碑完成”不是页面演示，而是：

- 一句话创意可以形成结构化项目。
- 全部核心实体被持久化。
- Mock 资产经过真实任务队列和状态机。
- 可以合成一条完整可播放视频。
- 可以暂停、恢复、失败重试、替换一个镜头并重新导出。
- 可以展示成本模拟与 Evidence。
- 自动测试证明关键路径可重复。

### 时间性质

“约 2 周”是目标，不是要求以删除测试或削弱范围来强行满足的硬截止日期。

---

## DEC-015：真实生成 MVP 目标约 7–8 周

**状态：** Accepted

### 决策

在 Mock 闭环稳定之后，目标用约 7–8 周完成真实生成 MVP。

### 完成标准

必须满足 `INTENT.md` 的 Definition of Done，而不是只接通视频 API。

### 影响周期的因素

- Provider 可用性
- 角色一致性
- 中文口型
- QC 准确性
- 外部 API 限流
- 安全审核
- 基准样片回归结果

周期变化必须基于 Evidence 说明，不能只报告“进度百分比”。

---

## DEC-016：只处理有合法使用权的真人与素材

**状态：** Accepted

### 决策

- 用户上传真人肖像、声音、动作或影视素材时必须确认拥有使用权。
- 系统不得默认抓取或使用来源不明的真人参考素材。
- 触发内容安全、肖像或声音冒充风险时必须停止或进入审核。
- 不提供第三方水印移除能力。
- 发布行为必须由用户最终确认。

---

## DEC-017：Evidence-Gated 完成判定

**状态：** Accepted

### 决策

任何功能、阶段或里程碑的“完成”必须附带可核查证据，例如：

- 自动化测试结果
- 端到端运行日志
- 状态迁移记录
- 截图
- 视频与音频产物
- QC 报告
- 成本事件
- 失败注入与恢复结果
- 可复现步骤

以下内容不能单独证明完成：

- Codex 口头声明
- 文件数量
- 编译一次成功
- 单次演示成功
- VLM 给出的单一分数
- TODO 被删除

---

# 3. 尚未锁定、需要 Explore 的决策

下列事项尚未被本文件决定，应先调研和验证，不得由 Codex 随意选择后永久写死：

1. 第一批真实图片、视频、TTS、口型和音乐 Provider。
2. Provider 的中国大陆或目标部署区域可用性。
3. Temporal 的本地开发与部署方式。
4. 第一版身份验证是否使用本地单用户、轻量登录或外部 Auth。
5. 具体对象存储服务商。
6. VLM QC 的模型和评估方式。
7. 音乐素材的授权方案。
8. 内容审核 Provider 与规则。
9. 是否需要独立的 WebSocket/SSE 进度通道。
10. 首版部署环境与持续集成平台。

这些问题应在 Explore 报告中给出：

- 候选方案
- 官方文档证据
- 风险
- 成本
- 推荐结论
- 验证实验

---

# 4. 批准记录

```yaml
document: 02_DECISIONS.md
version: v0.1
status: approved
approved_by: Product Owner
approved_at: 2026-08-29
basis:
  - INTENT.md v0.1 approved
  - all recommended default decisions explicitly accepted
```
